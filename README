DateTime-Format-Exif

This module handles formatting and parsing of comma separated dates and times
as supported by Exif cameras.

E.g. 2011:08:14 11:13:25

INSTALLATION

To install this module, run the following commands:

	perl Makefile.PL
	make
	make test
	make install
